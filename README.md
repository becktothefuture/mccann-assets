# McCann Assets

Static distribution bundle for the McCann Webflow integration. The `dist/` directory is published automatically to GitHub Pages via the provided workflow so Webflow can consume the hosted `app.js` and `style.css` files.
